// Celsius to Fahrenheit conversion formula
// fahrenheit = celsius * 1.8 + 32

const celsius = prompt("Enter celceius value:");
const fahrenheit = celsius * 1.8 + 32;

console.log(`${celsius} degree celcius is equal to ${fahrenheit} degree fahrenheit`);